html.sty

	The 'html' LaTeX package, typically installed as part
	of the latex2html package.  It adds some macros that
	make it possible to test if HTML generation or regular
	latex processing has been selected by the user.  This
	copy will be used if the latex2html package has not
	been installed on the system.

